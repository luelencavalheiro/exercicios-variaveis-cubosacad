let raioCirculo = 1;
let areaCirculo = (Math.PI * Math.pow(raioCirculo, 2)).toFixed(2);
let comprimentoCirculo = (2 * Math.PI * raioCirculo).toFixed(2);

console.log(`Considerando um raio de ${raioCirculo}, a área do círculo será ${areaCirculo} e o comprimento será ${comprimentoCirculo}`);